import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'demo-angular7',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
